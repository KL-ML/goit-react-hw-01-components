import styled from '@emotion/styled';
//import { flexbox } from 'styled-system';
import {
    space,
    layout,
    color,
    typography,
    flexbox,
    grid,
    background,
    border,
    position,
    shadow,
} from 'styled-system';

export const Box = styled('div')(
    color,
    space,
    layout,
    typography,
    flexbox,
    grid,
    background,
    border,
    position,
    shadow,
);
